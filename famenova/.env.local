# Server
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://rexadmin:rexpassword123@localhost:27017/rex_agency?authSource=admin

# JWT
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRES_IN=7d

# Email (Gmail App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=rexagency0@gmail.com
SMTP_PASS=your-app-password-here

# Notifications sent to
NOTIFY_EMAIL=rexagency0@gmail.com

# Excel dataset path (relative to backend root)
EXCEL_PATH=./dataset/leads.xlsx

# MongoDB (Docker)
MONGO_USER=rexadmin
MONGO_PASS=rexpassword123
